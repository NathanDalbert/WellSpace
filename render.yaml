databases:
  - name: wellspace-postgres
    plan: free
    databaseName: wellspace
    user: wellspace_user

services:
  - type: web
    name: wellspace-app
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    startCommand: env
    healthCheck:
      path: /v3/api-docs
      initialDelaySeconds: 45
    envVars:
      - key: DB_URL
        fromDatabase:
          name: wellspace-postgres
          property: connectionString
      - key: POSTGRES_USER
        fromDatabase:
          name: wellspace-postgres
          property: user
      - key: POSTGRES_PASSWORD
        fromDatabase:
          name: wellspace-postgres
          property: password
      - key: SPRING_PROFILES_ACTIVE
        value: docker
      - key: APP_FRONTEND_URL
        sync: false
      - key: API_SECRET
        sync: false
      - key: MAIL_HOST
        sync: false
      - key: MAIL_PORT
        sync: false
      - key: MAIL_USERNAME
        sync: false
      - key: MAIL_PASSWORD
        sync: false
      - key: MAIL_SMTP_AUTH
        sync: false
      - key: MAIL_SMTP_STARTTLS_ENABLE
        sync: false
      - key: GEMINI_API_KEY
        sync: false